#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Пример кода 04: конвейер обработки MarkItDown в RAGTool
Показать полный процесс любого формата→Markdown→Блокировка→Векторизация.
"""

import os
import time
import tempfile
from hello_agents.tools import RAGTool
from dotenv import load_dotenv
load_dotenv()

class MarkItDownPipelineDemo:
    """Демо-класс конвейера обработки MarkItDown"""
    
    def __init__(self):
        self.rag_tool = RAGTool(
            knowledge_base_path="./demo_rag_kb",
            rag_namespace="markitdown_demo"
        )
        self.temp_dir = tempfile.mkdtemp()
    
    def create_sample_documents(self):
        """Создавайте образцы мультиформатных документов"""
        print("📄 Создавайте образцы многоформатных документов.")
        print("=" * 50)
        
        # Создать документ Markdown
        markdown_content = """# Руководство по программированию на Python

## Базовый синтаксис
Python — это интерпретируемый язык программирования высокого уровня.

### Переменные и типы данных
- Целое число: `42`
- Строка: `"Привет, мир"`
- Список: `[1, 2, 3]`

### Определение функции
``` питон
защита приветствие(имя):
    return f"Привет, {имя}!"
```

## Объектно-ориентированное программирование
Python поддерживает парадигму объектно-ориентированного программирования.

### Определение класса
``` питон
Класс Человек:
    def __init__(я, имя):
        self.name = имя
    
    защита Say_hello (я):
        return f"Привет, я {self.name}"
```
"""
        
        # Создать HTML-документ
        html_content = """<!DOCTYPE html>
<html>
<голова>
    <title>Основы веб-разработки</title>
</голова>
<тело>
    <h1>Основы HTML</h1>
    <p>HTML — это язык разметки гипертекста, используемый для создания структуры веб-страниц. </p>
    
    <h2>Общие теги</h2>
    <ул>
        <li>h1–h6: тег заголовка.</li>
        <li>p: тег абзаца</li>
        <li>div: тег контейнера</li>
        <li>span: встроенный тег</li>
    </ul>
    
    <h2>Стили CSS</h2>
    <p>CSS используется для управления стилем и макетом веб-страниц. </p>
    <код>
        тело {семейство шрифтов: Arial, без засечек; }
        .container { максимальная ширина: 1200 пикселей; маржа: 0 авто; }
    </код>
</тело>
</html>"""
        
        # Создать документ JSON
        json_content = """{
    "project": "HelloAgents",
    "версия": "1.0.0",
    "description": "Среда разработки агентов ИИ",
    "особенности": [
        «система памяти»,
        «ТРЯПНЫЙ поиск»,
        «Интеграция инструментов»,
        «Мультимодальная поддержка»
    ],
    "компоненты": {
        "память": {
            «типы»: [«рабочий», «эпизодический», «смысловой», «перцептивный»],
            "хранилище": ["SQLite", "Qdrant", "Neo4j"]
        },
        "тряпка": {
            "форматы": ["PDF", "Word", "Excel", "HTML", "Markdown"],
            "pipeline": ["MarkItDown", "Чанкирование", "Встраивание", "Хранилище"]
        }
    }
}"""
        
        # Создать CSV-документ
        csv_content = """Название, тип, важность, описание
Рабочая память, временное хранилище, 0,7, хранит временную информацию для текущего сеанса.
Эпизодическая память, запись событий, 0,8, запись конкретных событий и переживаний
Семантическая память, хранилище знаний, 0,9, хранит концептуальные знания и правила.
Перцептивная память, мультимодальная, 0,6, обработка перцептивных данных, таких как изображение и звук.
Векторный поиск, технические компоненты, 0,8, поиск на основе семантического сходства
Граф знаний, технические компоненты, 0.9, структурированное представление связей сущностей"""
        
        # Сохранить документ во временный каталог
        documents = {
            "python_guide.md": markdown_content,
            "web_basics.html": html_content,
            "project_info.json": json_content,
            "memory_types.csv": csv_content
        }
        
        file_paths = {}
        for filename, content in documents.items():
            file_path = os.path.join(self.temp_dir, filename)
            with open(file_path, 'w', encoding='utf-8') as f:
                f.write(content)
            file_paths[filename] = file_path
            print(f"✅ Создать документ: {имя файла}")
        
        return file_paths
    
    def demonstrate_markitdown_conversion(self, file_paths):
        """Демонстрация процесса преобразования MarkItDown"""
        print("\n🔄 Демонстрация процесса конвертации MarkItDown")
        print("-" * 50)
        
        print("Процесс обработки MarkItDown:")
        print("1. 📄 Определить формат документа")
        print("2. 🔄 Конвертировать в Markdown")
        print("3. 📝 Сохраняйте структурную информацию")
        print("4. ✨ Единый формат вывода")
        
        conversion_results = {}
        
        for filename, file_path in file_paths.items():
            print(f"\nОбработка документов: {имя файла}")
            print(f"Исходный формат: {os.path.splitext(имя файла)[1]}")
            
            start_time = time.time()
            
            # Используйте RAGTool для добавления документов, MarkItDown будет вызываться внутри
            result = self.rag_tool.run({"action":"add_document", 
                                         "file_path":file_path})
            
            process_time = time.time() - start_time
            
            print(f"Результат обработки: {result}")
            print(f"Время обработки: {process_time:.3f} секунд.")
            print(f"✅ {имя файла} → Markdown → Разбивка на части → Векторизация")
            
            conversion_results[filename] = {
                "result": result,
                "time": process_time
            }
        
        return conversion_results
    
    def demonstrate_markdown_chunking(self):
        """Демонстрирует интеллектуальное разбиение на блоки на основе Markdown"""
        print("\n📊 Демонстрация умного разбиения на блоки на основе Markdown")
        print("-" * 50)
        
        print("Стратегия разбиения Markdown:")
        print("• 🏷️ Осознание иерархии заголовков — использование структур #, ##, ###.")
        print("• 📝 Сохранение семантики абзацев — сохранение целостности контента.")
        print("• 🔢 Точный контроль токена — модель внедрения адаптации.")
        print("• 🔗 Интеллектуальная стратегия перекрытия — избегайте потери информации")
        
        # Добавьте сложный документ Markdown, чтобы продемонстрировать фрагментирование.
        complex_markdown = """#стек технологий искусственного интеллекта

## Основы машинного обучения

### Обучение под присмотром
Обучение с учителем использует размеченные данные для обучения моделей, включая задачи классификации и регрессии.

#### Алгоритм классификации
- Логистическая регрессия: используется для задач бинарной и мультиклассификации.
- Дерево решений: древовидная структура, основанная на сегментации признаков.
- Случайный лес: ансамблевый метод множественных деревьев решений.
- Машина опорных векторов: поиск оптимальной разделяющей гиперплоскости.

#### Алгоритм регрессии
- Линейная регрессия: установление линейной связи между функциями и целями.
- Полиномиальная регрессия: работа с нелинейными отношениями.
- Ридж-регрессия: добавлена линейная регрессия с регуляризацией L2.

### Обучение без присмотра
Обучение без учителя обнаруживает закономерности и структуры на основе неразмеченных данных.

#### Алгоритм кластеризации
- K-средние: метод кластеризации на основе расстояния.
- Иерархическая кластеризация: построение древовидной структуры кластеризации.
- DBSCAN: алгоритм кластеризации на основе плотности.

#### Алгоритм уменьшения размерности
- PCA: анализ главных компонентов, уменьшение линейной размерности
- t-SNE: нелинейное уменьшение размерности, подходящее для визуализации.
- UMAP: уменьшение размерности с сохранением локальной и глобальной структуры.

## Глубокое обучение

### Основы нейронной сети
Нейронная сеть лежит в основе глубокого обучения и моделирует нейронную структуру человеческого мозга.

#### Основные компоненты
- Нейрон: базовая вычислительная единица.
- Функция активации: ввести нелинейность
- Функция потерь: измеряет ошибку прогноза.
- Оптимизатор: обновление параметров сети.

### Общая архитектура
- CNN: сверточная нейронная сеть, подходящая для обработки изображений.
- RNN: рекуррентная нейронная сеть, обрабатывающая данные последовательности.
- LSTM: сеть долговременной краткосрочной памяти, решение проблемы исчезновения градиента.
- Трансформер: механизм внимания, обработка длинных последовательностей

## Обработка естественного языка

### Предварительная обработка текста
- Токенизация: разделение текста на лексические единицы.
- Разметка частей речи: выявление грамматической роли слов
- Распознавание именованных объектов: извлечение таких объектов, как имена людей и географические названия.
- Анализ тональности: определить эмоциональную направленность текста.

### Языковая модель
- N-грамма: языковая модель, основанная на статистике.
- Word2Vec: обучение представлению векторов слов
- BERT: представление двунаправленного энкодера.
- GPT: генеративная предварительно обученная модель.
"""
        
        print(f"\n📝 Добавляйте сложные документы Markdown для сегментированного тестирования...")
        result = self.rag_tool.run({"action":"add_text",
                                     "text":complex_markdown,
                                     "document_id":"ai_tech_stack",
                                     "chunk_size":800,
                                     "chunk_overlap":100})
        
        print(f"Частичные результаты: {result}")
        
        # Поиск на основе структуры теста
        print(f"\n🔍 Тестовый поиск на основе структуры Markdown:")
        
        search_queries = [
            ("Алгоритм контролируемого обучения", "Тестирование получения содержимого вторичного заголовка"),
            ("Основы нейронной сети", "Тестирование межуровневого поиска контента"),
            ("BERT GPT", "Тестирование поиска конкретной технологии"),
            ("Уменьшение размерности кластеризации", "Поиск концепций, связанных с тестированием")
        ]
        
        for query, description in search_queries:
            print(f"\nЗапрос: '{query}' ({description})")
            search_result = self.rag_tool.run({"action":"search",
                                                "query":query,
                                                "limit":2})
            print(f"Результаты поиска: {search_result[:200]}...")
    
    def demonstrate_embedding_optimization(self):
        """Демонстрирует предварительную обработку Markdown для встраивания"""
        print("\n🎯 Демонстрация предварительной обработки Markdown для встраивания")
        print("-" * 50)
        
        print("Оптимизация предварительной обработки Markdown:")
        print("• 🏷️ Удалите теги форматирования и сохраните семантическое содержимое.")
        print("• 🔗 Обработка формата ссылок и сохранение текста ссылки.")
        print("• 💻 Очищайте блоки кода и сохраняйте содержимое кода.")
        print("• 🧹 Очистите лишнее пустое пространство и оптимизируйте векторное представление.")
        
        # Демонстрационное сравнение до и после предварительной обработки
        raw_markdown = """## Пример кода

Это **важная** функция Python:

``` питон
защита Process_data (данные):
    \"\"\"Функции обработки данных\"\"\"
    вернуть [item.strip() для элемента в данных, если элемент]
```

Для получения дополнительной информации обратитесь к [Официальной документации](https://docs.python.org).

*Примечание*: Эта функция автоматически отфильтровывает нулевые значения.
"""
        
        print(f"\n📝 Исходное содержимое Markdown:")
        print(raw_markdown)
        
        # Добавлено в систему RAG, предварительная обработка будет выполняться внутри.
        result = self.rag_tool.run({"action":"add_text",
                                     "text":raw_markdown,
                                     "document_id":"preprocessing_demo"})
        
        print(f"\n✅ Предварительная обработка и добавление завершены: {result}")
        
        # Проверьте эффект извлечения после предварительной обработки
        print(f"\n🔍 Проверьте эффект извлечения после предварительной обработки:")
        search_result = self.rag_tool.run({"action":"search",
                                            "query":"Функции Python для обработки данных",
                                            "limit":1})
        print(f"Результаты поиска: {search_result}")
    
    def demonstrate_pipeline_performance(self):
        """Демонстрация производительности конвейера обработки"""
        print("\n⚡ Демонстрация производительности конвейера обработки")
        print("-" * 50)
        
        print("Показатели теста производительности:")
        print("• 📄 Скорость конвертации документов")
        print("• 📊 Эффективность обработки блоков")
        print("• 🎯 Время векторизации")
        print("• 💾 Операция хранения требует времени")
        
        # Тест производительности пакетной обработки
        batch_texts = [
            f"Документ тестирования пакетной обработки {i+1}: это образец документа, используемый для проверки производительности конвейера обработки MarkItDown."
            f"Документы содержат контент в различных форматах, включая структурированную информацию, такую ​​как заголовки, абзацы, списки и т. д."
            f"Пакетная обработка позволяет оценить общую производительность системы." 
            for i in range(10)
        ]
        
        print(f"\n⏱️ Тест производительности пакетной обработки (10 документов):")
        start_time = time.time()
        
        batch_result = self.rag_tool.batch_add_texts(
            batch_texts,
            document_ids=[f"perf_test_{i+1}" for i in range(10)]
        )
        
        batch_time = time.time() - start_time
        
        print(f"Результаты пакетной обработки: {batch_result}")
        print(f"Общее затраченное время: {batch_time:.3f} секунд.")
        print(f"Среднее значение на документ: {batch_time/10:.3f} секунд.")
        
        # Получить окончательную статистику
        stats = self.rag_tool.run({"action":"stats"})
        print(f"\n📊 Итоговая статистика: {stats}")

def main():
    """основная функция"""
    print("🔄 Демонстрация конвейера обработки MarkItDown от RAGTool")
    print("Показать полный процесс любого формата→Markdown→Блокировка→Векторизация.")
    print("=" * 70)
    
    try:
        demo = MarkItDownPipelineDemo()
        
        # 1. Создайте образцы мультиформатных документов.
        file_paths = demo.create_sample_documents()
        
        # 2. Демонстрация процесса преобразования MarkItDown
        conversion_results = demo.demonstrate_markitdown_conversion(file_paths)
        
        # 3. Продемонстрировать интеллектуальную блокировку на основе Markdown
        demo.demonstrate_markdown_chunking()
        
        # 4. Продемонстрировать оптимизацию предварительной обработки для встраивания
        demo.demonstrate_embedding_optimization()
        
        # 5. Демонстрация производительности конвейера обработки
        demo.demonstrate_pipeline_performance()
        
        print("\n" + "=" * 70)
        print("🎉 Демонстрация конвейера обработки MarkItDown завершена!")
        print("=" * 70)
        
        print("\n✨ Основные функции конвейера обработки:")
        print("1. 🔄 Единый формат — Любой формат → Стандартизация Markdown")
        print("2. 🏗️ Сохранение структуры — сохраните логическую структуру документа.")
        print("3. 📊 Интеллектуальная блокировка — семантическая сегментация на основе структуры Markdown.")
        print("4. 🎯 Оптимизация внедрения — предварительная обработка для векторизации")
        print("5. ⚡ Эффективная обработка — пакетная обработка и оптимизация производительности.")
        
        print("\n🎯 Технические преимущества:")
        print("• Унифицированная обработка – один процесс для всех форматов.")
        print("• С учетом структуры — воспользуйтесь всеми преимуществами структурной информации Markdown.")
        print("• Семантическое сохранение – поддержание семантической целостности при преобразовании формата.")
        print("• Оптимизация поиска — текстовое представление, оптимизированное для векторного поиска.")
        
        # Очистить временные файлы
        import shutil
        shutil.rmtree(demo.temp_dir)
        print(f"\n🧹 Очистка временных файлов: {demo.temp_dir}")
        
    except Exception as e:
        print(f"\n❌ Во время демонстрации произошла ошибка: {e}")
        import traceback
        traceback.print_exc()

if __name__ == "__main__":
    main()