"""
Пример сотрудничества команды разработчиков программного обеспечения AutoGen
"""

import os
import asyncio
from typing import List, Dict, Any
from dotenv import load_dotenv

# Загрузить переменные среды
load_dotenv()

# Сначала протестируйте версию, используя клиент OpenAI.
from autogen_ext.models.openai import OpenAIChatCompletionClient
from autogen_agentchat.agents import AssistantAgent, UserProxyAgent
from autogen_agentchat.teams import RoundRobinGroupChat
from autogen_agentchat.conditions import TextMentionTermination
from autogen_agentchat.ui import Console

def create_openai_model_client():
    """Создайте клиент модели OpenAI для тестирования."""
    return OpenAIChatCompletionClient(
        model=os.getenv("LLM_MODEL_ID", "gpt-4o"),
        api_key=os.getenv("LLM_API_KEY"),
        base_url=os.getenv("LLM_BASE_URL", "https://api.openai.com/v1")
    )

def create_product_manager(model_client):
    """Создайте агента менеджера по продукту"""
    system_message = """Вы опытный менеджер по продукту, специализирующийся на анализе требований и планировании проектов для программных продуктов.

В ваши основные обязанности входит:
1. **Анализ требований**: углубленное понимание потребностей пользователей, определение основных функций и граничных условий.
2. **Техническое планирование**: Разработайте четкий план технической реализации, основанный на потребностях.
3. **Оценка рисков**: выявление потенциальных технических рисков и проблем с взаимодействием с пользователем.
4. **Координация и общение**. Эффективно общайтесь с инженерами и другими членами команды.

При получении задания на разработку проанализируйте его по следующей структуре:
1. Понимание и анализ требований
2. Разделение функциональных модулей
3. Предложения по техническому отбору
4. Внедрите расстановку приоритетов
5. Определение критериев приемки

Пожалуйста, ответьте кратко и ясно и скажите: «Попросите инженеров начать внедрение» после завершения анализа."""

    return AssistantAgent(
        name="ProductManager",
        model_client=model_client,
        system_message=system_message,
    )

def create_engineer(model_client):
    """Создайте агента инженера-программиста"""
    system_message = """Вы старший инженер-программист, специализирующийся на разработке Python и создании веб-приложений.

Ваш технический опыт включает в себя:
1. **Программирование на Python**: знание синтаксиса Python и передовых практик.
2. **Веб-разработка**: знание Streamlit, Flask, Django и других фреймворков.
3. **Интеграция API**. Имейте богатый опыт интеграции сторонних API.
4. **Обработка ошибок**: сосредоточьтесь на надежности кода и обработке исключений.

При получении задания на разработку, пожалуйста:
1. Тщательно проанализируйте технические требования
2. Выберите правильное техническое решение
3. Напишите полную реализацию кода
4. Добавьте необходимые комментарии и инструкции.
5. Рассмотрите крайние случаи и обработку исключений

Предоставьте полный работоспособный код и, когда закончите, скажите: «Пожалуйста, просмотрите его у специалиста по проверке кода»."""

    return AssistantAgent(
        name="Engineer",
        model_client=model_client,
        system_message=system_message,
    )

def create_code_reviewer(model_client):
    """Создайте агента проверки кода"""
    system_message = """Вы опытный специалист по проверке кода, уделяющий особое внимание качеству кода и передовым практикам.

Основные моменты вашего обзора включают в себя:
1. **Качество кода**. Проверьте читаемость, удобство сопровождения и производительность кода.
2. **Безопасность**: выявление потенциальных уязвимостей безопасности и точек риска.
3. **Лучшие практики**. Убедитесь, что код соответствует отраслевым стандартам и передовым практикам.
4. **Обработка ошибок**: проверьте целостность и рациональность обработки исключений.

Процесс проверки:
1. Внимательно прочитайте и поймите логику кода.
2. Проверьте стандарты и лучшие практики кодирования.
3. Определите потенциальные проблемы и точки улучшения.
4. Предоставьте конкретные предложения по изменениям.
5. Оцените общее качество кода

Оставьте конкретные комментарии к обзору и по завершении скажите: «Проверка кода завершена, пожалуйста, проверьте пользовательский агент»."""

    return AssistantAgent(
        name="CodeReviewer",
        model_client=model_client,
        system_message=system_message,
    )

def create_user_proxy():
    """Создайте агент пользовательского агента"""
    return UserProxyAgent(
        name="UserProxy",
        description="""Пользовательский агент несет ответственность за следующие обязанности:
1. Предложение потребностей в развитии от имени пользователей
2. Выполните окончательную реализацию кода
3. Убедитесь, что функциональность соответствует ожиданиям.
4. Предоставляйте пользователям отзывы и предложения.

Пожалуйста, ответьте «ПРЕКРАТИТЬ», когда тестирование будет завершено.""",
    )

async def run_software_development_team():
    """Организуйте совместную работу команды разработчиков программного обеспечения"""
    
    print("🔧 Инициализация клиента модели...")
    
    # Сначала протестируйте, используя стандартный клиент OpenAI.
    model_client = create_openai_model_client()
    
    print("👥Создание агентской команды...")
    
    # Создать команду агентов
    product_manager = create_product_manager(model_client)
    engineer = create_engineer(model_client)
    code_reviewer = create_code_reviewer(model_client)
    user_proxy = create_user_proxy()
    
    # Добавить условие завершения
    termination = TextMentionTermination("TERMINATE")
    
    # Создать командный чат
    team_chat = RoundRobinGroupChat(
        participants=[
            product_manager,
            engineer, 
            code_reviewer,
            user_proxy
        ],
        termination_condition=termination,
        max_turns=20,  # Увеличьте максимальное количество раундов.
    )
    
    # Определить задачи разработки
    task = """Нам нужно разработать приложение для отображения цен на биткойны. Конкретные требования заключаются в следующем:

Основные функции:
- Отображает текущую цену биткойнов (USD) в режиме реального времени.
- Отображать 24-часовую тенденцию изменения цен (увеличение и уменьшение, а также величину увеличения и уменьшения).
- Обеспечить функцию обновления цен

Технические требования:
- Создание веб-приложений с использованием фреймворка Streamlit.
- Интерфейс простой, красивый и удобный
- Добавлена правильная обработка ошибок и статус загрузки.

Попросите команду работать вместе над выполнением этой задачи, от анализа требований до окончательной реализации."""
    
    # Выполнять командную работу
    print("🚀 Начните сотрудничество с командой разработчиков программного обеспечения AutoGen...")
    print("=" * 60)
    
    # Используйте консоль для отображения процесса разговора
    result = await Console(team_chat.run_stream(task=task))
    
    print("\n" + "=" * 60)
    print("✅Командная работа завершена!")
    
    return result

# Основная запись программы
if __name__ == "__main__":
    try:
        # Запустите асинхронный процесс совместной работы
        result = asyncio.run(run_software_development_team())
        
        print(f"\n📋 Итоги сотрудничества:")
        print(f"- Количество участвующих агентов: 4")
        print(f"- Статус завершения задачи: {'успех', если результат еще 'требует дальнейшей обработки'}")
        
    except ValueError as e:
        print(f"❌ Ошибка конфигурации: {e}.")
        print("Пожалуйста, проверьте правильность конфигурации в файле .env.")
    except Exception as e:
        print(f"❌ Ошибка выполнения: {e}")
        import traceback
        traceback.print_exc()



